You are working in a Next.js 15 pure frontend project with the rules defined in CLAUDE.md and my global personal memory (2026 best practices).

### PHASE 1: Set Up the 3 Core Agents
First, create the following three agent files inside the `.claude/agents/` folder. Use the exact filenames:

1. **architect.md**
2. **frontend-dev.md**
3. **reviewer.md**

Each agent must respect:
- The exact folder structure and tech stack from CLAUDE.md (Next.js 15 App Router, Tailwind v4 via @tailwindcss/postcss, shadcn/ui, src/lib/api.ts as SINGLE source of all HTTP calls, TypeScript strict, etc.).
- Distinctive non-generic UI (frontend-design skill).
- All API calls must go through src/lib/api.ts with proper loading/error/empty states.
- PocketBase will be used as the backend (http://127.0.0.1:8090 or the value in NEXT_PUBLIC_POCKETBASE_URL).

**Architect Agent** — Responsible for planning, creating detailed SPEC.md, deciding file structure, and defining API contracts.

**Frontend-Dev Agent** — Responsible for implementing code, creating components, pages, hooks, and following shadcn/ui + Tailwind v4 rules. Only use "use client" when necessary.

**Reviewer Agent** — Responsible for checking code against CLAUDE.md, accessibility, Core Web Vitals, design quality, type safety, and consistency with api.ts wrapper. It must suggest fixes and approve only when everything meets standards.

After creating the three agent files, confirm with: "✅ 3 agents (Architect, Frontend-Dev, Reviewer) have been created successfully."

### PHASE 2: Build the Complete App
Now use the agents in this order: Architect → Frontend-Dev → Reviewer (repeat review loop if needed).

Project Name: **[progress app]** (replace with your actual name, e.g. HabitForge)

**Core Requirements:**
- Make it a **Progressive Web App (PWA)**: Add manifest.json, service worker (offline support), installable on mobile, display: standalone, proper icons and theme colors.
- Use PocketBase as backend for JWT-style authentication (email/password).
  - Login/Register pages with react-hook-form + Zod validation.
  - Protected routes (client + server-side checks).
  - Use PocketBase SDK properly with cookie-based auth (exportToCookie / httpOnly where possible).
  - src/lib/api.ts or src/lib/pocketbase.ts as the central client (respect CLAUDE.md rule: all calls must go through the wrapper).
- Include all mid-level frontend skills:
  - TanStack Query v5 for data fetching, mutations, caching, optimistic updates.
  - Zustand v5 for client state (theme, UI preferences).
  - Dark/light mode toggle saved in localStorage.
  - Loading, error, and empty states on every data call.
  - Distinctive, accessible UI with shadcn/ui + Tailwind v4 (enforce frontend-design skill — no AI slop).
- Main features (adapt to project name, e.g. for HabitForge: habit creation, daily logging, streaks, dashboard, profile).
- Add a simple README.md section explaining how to run PocketBase locally and test the PWA on mobile.

**Workflow:**
1. Architect creates a detailed SPEC.md with architecture, pages, API contracts, and PWA setup.
2. Frontend-Dev implements everything step by step.
3. Reviewer checks quality after major parts and at the end.
4. Final output: Run `npm run build` successfully and confirm Lighthouse score is high.

After everything is complete, reply with:
"✅ [PROJECT_NAME] is fully built with 3 agents, PocketBase JWT/cookie auth, PWA support, and all mid-level frontend features."

Start PHASE 1 now.